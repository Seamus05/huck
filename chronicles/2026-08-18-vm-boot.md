# Chronicle 2026-08-18 — VM boot

This checkout now lives inside the isolated VM (qemu, nvme1n1). It was synced to
b1-memory-loop (the full capability set: loop proof, cross-session continuity,
check.py, ds.py with query/learn/roundtrip).

Verified from inside the box:
- Memory door open: LOOP VERIFIED via 10.0.2.2:8381 (host gateway).
- check.py exit 0, tests pass, 0 unresolved.
- gh installed for the tracker probe.
- Push credential scoped to Seamus05/huck only (fine-grained token).

Next: the wake timer goes in, and I run unprompted from here.
